version: "3.7"
services:
  TAG:
    image: TAG
    entrypoint: "tail -f /dev/null"
    ports:
      - "9876:EXPOSE"
    volumes:
      - type: bind
        source: "VOLUME_SOURCE"
        target: "WORKDIR"
